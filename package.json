{
  "name": "finanz-ai",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @finanzia/db run build && cd apps/backend && pnpm start:dev"
  }
}
