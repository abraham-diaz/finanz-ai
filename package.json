{
  "name": "finanz-ai",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @finanzia/db run build && concurrently -n backend,frontend -c blue,green \"pnpm --filter backend run start:dev\" \"pnpm --filter frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
